from setuptools import setup, find_packages

setup(name='llm_utils',
      install_requires=[],
      version="0.1.0",
      author="hackiey",
      packages=find_packages(),
      include_package_data=True)
